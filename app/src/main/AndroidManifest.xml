<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robincxiao.androidcanvas" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".xfermodes.XfermodesActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".shader.ShaderActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".path.PathEffectActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".path.PathMeasureActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>